---

# File: playbooks/hosts/red.secretsmgmt.tk.yml

- name: Configure host "red.secretsmgmt.tk"
  hosts: red

  vars:
    playbooks_root: "{{ lookup('env', 'PBR') }}"

  vars_files:
   - "{{ playbooks_root }}/vars/global.yml"

  remote_user: "ansible"
  become: yes

  roles:
    - { role: base }
    - { role: postfix }
    - { role: docker }
    - { role: consul }
    - { role: python-virtualenv, use_sphinx: false }
    - { role: nginx }
    - { role: rabbitmq }
    - { role: postfix }

  handlers:
   - include: "{{ handlers_path }}/restart_services.yml"

# vim: ft=ansible :
